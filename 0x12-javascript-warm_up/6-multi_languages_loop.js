#!/usr/bin/node
// Create an array of strings
const langs = ["c is fun", "Python is cool", "JavaScript is amazing"];
// Loop through the the array and print each line
for (const lang of langs) {
        console.log(lang);
}
